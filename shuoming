OnlineAdmin中admin == 0 表示用户不在线，OnlineUser同理
创建所有类型的窗口，其routerid都<0
用户数据，管理员数据的id都>0

管理员id，用户id与窗口id相同

当登录界面登录后，主窗口才会被创建
，当退出登陆后，主窗口被销毁

+++++++++++++++++++++++
数据访问层：
save包：根据save包内的枚举定义的字段，从数据库获得对应字段的数据
       注意：按数据库中的字段定义，不得随意命名
       如何添加新的表在save包？
       1、添加新的枚举类，命名按数据库表的名称；
       2、添加新的 (数据表名)Form ,内容照抄DeviceForm即可
       3、打开tool包，添加loadAllFormData（）方法到线程中，启用定时刷新数据
       功能使用：管理员查询所有用户信息

       实现管理员增加删除修改操作：1、两标记：标记字段、查询该字段下的id
统一写数据库访问层时候，类命名按数据库表命名，操作方式用包封装