package Serve.Strategy;

import javax.swing.table.DefaultTableModel;

public class UserModel implements ModelProcess{
    /// 生成用户数据模型
    ///
    /**
     * 添加用户数据到模型中
     * */
    @Override
    public void add(DefaultTableModel defaultTableModel) {

    }
}
